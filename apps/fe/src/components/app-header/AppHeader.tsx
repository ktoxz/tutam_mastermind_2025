import React from 'react';
import AppSection from '../app-section/AppSection';

type Props = {};

function AppHeader({}: Props) {
	return <AppSection>AppHeader</AppSection>;
}

export default AppHeader;
