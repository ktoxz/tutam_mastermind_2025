import { Metadata } from 'next';

const metadata: Metadata = {
	title: 'Tu Tam Auth',
	description: 'Tu Tam Authentication pages',
	keywords: ['auth', 'authentication', 'login', 'signup'],
};

export default metadata;
